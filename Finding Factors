import math
def print_numbers(n):
    list=[]
    for i in range(1,int(math.sqrt(n)+1)):
        if n%i==0:
            list.append(i)
        if n/i!=i:
            list.append(int(n/i))
    print("Factors are : ",list)

n=int(input("Enter the value :"))
print_numbers(n)
